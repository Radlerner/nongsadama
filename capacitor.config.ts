import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nongsadama.myapp',
  appName: 'NongsaDaMa',
  loggingBehavior: 'none',
  webDir: 'dist'
};

export default config;
