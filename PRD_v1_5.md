# 농사다마 PRD v1.5 — 지도·음성 중심 직관 UX 개정안 (2차 초안)

**작성일**: 2026-07-29 야간 (1차) → 독립 검토 P0 3건·P1 6건 반영 (2차)
**문서 상태**: ⚠️ **승인 대기 2차 초안** — 제품 오너 결정 전 구현하지 않는다 (PRD 11.7)
**기준 문서**: PRD_v1_3.md(운영 원칙·품질 기준) + PRD_v1_4.md(매칭) 위의 증분 개정
**롤백 앵커**: `v0.3.0` (커밋 0d463eb, 독립 재검수 통과 상태. §9)

---

## 0. 이번 개정의 핵심 결정 (제품 오너 지시 요약)

> "지도로 보고, 텍스트 부담 없이, 포켓몬고처럼 직관으로. **10세도 쓸 수 있게.**
> 음성으로 고민을 말하면 분석해 ① 쉬운 건 게시판 ② 만나서 해결할 건 생활정보
> ③ 어려운 건 LLM+상담기관 ④ 가벼운 친목은 근처 active 이웃 호출로."

이 지시를 4주 데모의 현실(잔여 ~1주, 무료 플랜, 1인 개발)과 취약 사용자 안전 위에서
**단계화**했다. 전부 한 번에 구현하지 않는다.

### 유지되는 불변 원칙 (v1.3 §9 · v1.4 §4)
1. 정확한 사용자 위치를 수집·저장·공개하지 않는다 — 지도의 인적 정보는 데모에서 **표시하지 않음**(§4)
2. 모든 노출은 opt-in + RLS 강제
3. 국가·언어·작목 비하드코딩 / 4. 무료 플랜(유료 API는 명시 승인 전 금지)
5. **음성 원본을 우리 서버에 저장하지 않는다** (§3.2 — 단, 브라우저 STT의 제3자 전송 고지 필요)

---

## 1. 비전 개정 — "직관 우선(Intuition-First)"

포켓몬고 레퍼런스의 본질: **"지도가 곧 세계, 큰 버튼 하나로 행동."**

**10세 사용성 원칙** (신규 화면 완료 조건, §7에서 정량화):
- 화면당 핵심 행동 1개, 주요 행동 56px+ / 그 외 44px+
- 아이콘·이모지 우선, 텍스트는 보조(번역 키 유지). **색과 아이콘을 병행**(색각 이상·저사양 이모지 렌더링 대비)
- 읽지 않아도 진행 가능, 실패해도 안전(뒤로 가기 명확, 파괴적 행동 2단계)
- 음성 입력은 가산점(progressive enhancement), TTS 안내는 v1.1 검토(§3.3 단계표)

---

## 2. 새 정보구조(IA)

### 2.1 현행 vs 개정

| | 현행 (v1.4) | 개정 (v1.5 데모) |
|---|---|---|
| 홈 | 이웃 섹션+바로가기 | **지도가 곧 홈** (생활정보 핀) |
| 하단 탭 | 홈·게시판·생활정보·내정보 | **지도 · 게시판 · 🎤말하기(중앙) · 이웃 · 내정보** |
| 생활정보 | 목록 탭 | 지도 핀 + 하단 시트(목록 뷰는 폴백 겸용) |
| 진입 | 랜딩→언어·지역 선택 | 동일 (유지) |

### 2.2 개정 메뉴 트리 (데모 범위)

```text
지도 (홈, 비로그인 열람 가능)
├─ 핀: 생활정보 🏥🛒🏛🚌ℹ️🆘 — 검수 실좌표는 실좌표 핀, 없으면 읍·면 중심 묶음 핀+"위치 검수 전" 배지
├─ 상단: 카테고리 아이콘 필터 바
├─ 핀 탭 → 하단 시트(아이콘+이름+신선도 배지) → 기존 상세 화면
└─ (게시글·이웃 핀은 v1.1 결정 사항 — §4 참조. 데모 지도에 인적 정보 없음)

🎤 말하기 (중앙 큰 버튼 — 고민 라우터 단계 A, §3)
├─ 큰 아이콘 4택: 💬 물어보고 싶어요 / 📍 가야 할 곳 찾아요 / 🆘 어려운 일이 있어요 / ☕ 이웃과 얘기하고 싶어요
├─ (지원 브라우저) 음성 입력 → 텍스트 확인 → 사용자가 아이콘으로 확정
├─ 모든 결과 화면 하단: 상시 도움 배너(☎1345 · 긴급 112 · 폭력 피해 1577-1366)
└─ 결과: ① 게시판 글쓰기 프리필(게시 전 공개 경고 필수) ② 생활정보 해당 카테고리
        ③ 상담기관(🆘) 카드 ④ 도움요청(help) 글쓰기 프리필(안전 문구+신고 링크)

게시판 (유지 + 'help' 카테고리 추가)
이웃 (유지 — 목록·매칭 점수. presence "활동 중"은 v1.1)
내 정보 (유지)
```

### 2.3 제품 오너 확인 목록 (§10에 통합)

---

## 3. 🎤 고민 라우터

### 3.1 4-way 분류 (지시 그대로)

| 분류 | 신호 예 | 연결 |
|---|---|---|
| ① 쉬움—물어보면 됨 | 사용법·소소한 질문 | 게시판 글쓰기(프리필) |
| ② 쉬움—가서 해결 | 병원·장보기·서류·교통 | 생활정보(해당 카테고리) |
| ③ 어려움—전문 도움 | 임금·계약·산재·폭력·비자 | **상담기관 카드 최우선** (+v1.1 LLM 보조) |
| ④ 가벼움—사람이 그리움 | 심심함·외로움·친목 | 도움요청 글(help) — LLM 대신 **사람** |

### 3.2 안전 원칙 (협상 불가) — 단계 A에서의 실행 수단 포함

독립 검토 P0-3 반영: 단계 A는 사용자가 스스로 분류하므로 "감지"에 기댈 수 없다.
따라서 다음을 **단계 A의 완료 조건**으로 명시한다:
1. **상시 도움 배너**: 라우터의 모든 결과 화면(①②④ 포함)에 ☎1345(외국인종합안내)·**112**(긴급)·
   **1577-1366**(폭력 피해, 다국어) 표시 — 잘못 분류해도 ③ 경로가 항상 보인다.
2. **게시 전 공개 경고**: 음성/입력 텍스트가 게시글로 프리필될 때 "이 글은 로그인하지 않은
   사람에게도 공개됩니다. 이름·농장·연락처를 쓰지 마세요" 확인 단계를 거친다.
3. LLM은 (v1.1 도입 시에도) 상담이 아니라 **검수된 기관으로의 안내 보조**까지만. 법률·의료 판단 미생성.
4. **음성 원본을 우리 서버에 저장하지 않는다.** 단, 브라우저 Web Speech API는 음성을
   브라우저 제공사 서버로 전송해 인식하므로(기기 내 처리가 아님) **마이크 사용 전 이를 고지**하고,
   개인정보 처리방침(§10-J)에 명시한다. 분류 판단 로그는 익명 통계 외 저장하지 않는다.

### 3.3 단계화

| 단계 | 내용 | 비용/의존 | 목표 |
|---|---|---|---|
| **A. 가이드 라우터** | 아이콘 4택 + 선택적 STT(사용자 확정) + §3.2 안전장치 | 0원 | **데모(P0')** |
| B. 키워드 보조 분류 | 다국어 키워드 사전으로 추천(사용자 확정) | 0원 | 데모 여유 시 |
| C. LLM 분류 | LLM 분류+위기 신호 감지. **위기 서술의 외부 API 전송 = 민감정보 제3자 제공** — 비용+별도 동의+처리방침 필요 | API 비용 | v1.1(승인) |
| D. LLM 상담 보조 | ③ 상황 요약·연결문(모국어) | 〃 + 기관 협의 | v1.1+ |
| TTS 안내 | 결과 화면 음성 읽어주기 | 브라우저 TTS 0원 | v1.1 검토 |

데모 성공 기준: "음성 인식률"이 아니라 **"아이콘 4택만으로 라우팅 완주"**(§7 정량 기준).

---

## 4. 지도 설계 (데모 = 생활정보 전용)

- **데모 범위**: 생활정보 핀 + 하단 시트 + 목록 폴백. **게시글·이웃 핀은 데모에서 제외.**
- **이웃 핀을 뺀 이유** (독립 검토 P0-2): v1.3 §3.3이 명시 제외한 "사용자 밀도 표시"와 충돌하고,
  비로그인 지도에 이웃 수를 보이려면 새 집계 경로가 필요하며, 파일럿 규모에서 "읍·면 N명"은
  국적 칩과 조합 시 개인 특정 위험이 있다. v1.1에서 도입하려면: 로그인+상호 동의자 한정,
  **최소 인원 임계(N<3이면 숨김)**, v1.3 §3.3의 명시 개정을 함께 결정해야 한다(§10-H).
- **라이브러리**: Leaflet(§10-B 승인 필요, v1.3 §7.6의 P1 예정을 승격). 타일: OSM 정책 준수
  무료 제공자 확인 후 선택. 성능: 코드 스플리팅을 선행 조건으로 승격(현 번들 160kB gz).
- **데이터**: `life_info.latitude/longitude`(검수 실좌표) 또는 읍·면 중심 묶음 핀. **좌표 날조 금지.**
- **폴백**: 타일 실패/저사양 → 기존 목록 뷰(현행 화면 재사용).

---

## 5. 이웃 호출 (데모 = help 글 프리필까지)

- **데모**: 라우터 ④ → `category='help'` 게시글 프리필. 완료 조건: (a) "공개 장소에서 만나세요"
  류 안전 문구 (b) **신고 수단 최소 1개**(운영자 이메일 링크 수준이라도) (c) §3.2 상시 배너.
- **presence(`last_seen_at` "활동 중")는 v1.1로 연기** (독립 검토 P1-2·P1-6): 도입 시 조건 —
  `is_matching_visible`과 **분리된 별도 opt-in 플래그(기본 off)**, 30분 단위 절사 저장(활동 패턴
  추적 완화), 같은 시/군 상호성 스코프, `neighbor_profiles` 뷰 재생성 시 **D-012 절차(revoke-then-
  grant+barrier) 필수**. 기존 "단일 동의 플래그 과겸용" 이월 이슈(TEST_CHECKLIST)와 함께 설계.
- **응답 경로의 한계(정직 고지)**: 댓글·DM이 없어 help 글에 대한 응답도 게시글뿐이다. 데모는
  "요청이 게시판에 보인다"까지를 검증하고, 실제 연결 UX(응답 알림 등)는 Realtime과 함께 v1.1.
- **아동 보호**: 낯선 사람 접촉을 유도하는 기능이므로, 10세 사용성을 표방하는 이상 아동-성인
  접촉(그루밍) 위험을 §10-I(만 14세 미만 정책)와 묶어 결정하기 전까지 ④의 대상을 성인 사용자로
  전제한다.

---

## 6. 상담기관 데이터 (라우팅 ③)

- DB 변경: `life_info` CHECK에 `'support'` 추가 (§9의 변경 목록 ①).
- 검수 원칙 유지: **출처+최종확인일 없는 실기관 정보를 싣지 않는다.** 제품 오너 지정 후보
  (농촌인력인권센터·외국인노동자(지원)센터·외국인인권센터 계열)는 명칭·관할·연락처·**존속 여부**
  확인 후 입력(일부 정부 지원 센터는 최근 예산·조직 변동 있음).
- 공공 번호(운영자 최종 확인 후 시딩): ☎**1345**(외국인종합안내)·**1350**(고용노동)·**112**(긴급)·
  **1577-1366**(폭력 피해, 다누리 다국어).
- **신선도 강화**: support 카테고리는 STALE 기준을 6개월 → **3개월**로 단축하고 "전화 전
  운영 여부를 확인하세요" 문구를 상세에 상시 노출(위기 자원의 오정보는 실해가 큼).

---

## 7. 접근성 완료 조건 (정량화)

v1.3 §11.4에 추가 — v1.3 §2.2 형식으로:
- 사용성 테스트 참가자에 **저문해력 성인 또는 아동 1명 이상** 포함(아동 참여 시 보호자 동의, §10-I)
- **5명 중 4명 이상**이 텍스트를 읽지 않고(진행 중 낭독 도움 없이) 라우터 4택 → 결과 도달 완주
- 주요 행동 56px+/기타 44px+, 색+아이콘 병행 — 화면 검사로 확인

---

## 8. 범위 재편성 (독립 검토 P1-6 축소안 채택)

**데모 잔여 기간 P0' (이 순서로, 밀리면 역순 절단):**

| 순위 | 데모에 추가 | 비고 |
|---|---|---|
| 1 | 🎤 라우터 단계 A + §3.2 안전장치(배너·게시 전 경고) | 0원, 안전 가치 최대 |
| 2 | support 카테고리 + 핫라인·기관 검수 시딩 | DB 변경 ① |
| 3 | 지도 홈(생활정보 핀+시트+목록 폴백) + 코드 스플리팅 | Leaflet 승인(§10-B) 필요 |
| 4 | 이웃 호출 = help 글 프리필(+안전 문구·신고 링크) | DB 변경 ② |

**v1.1로 연기**: 게시글·이웃 지도 핀(§4 조건), presence(§5 조건), Realtime 알림, LLM C/D, TTS,
키워드 분류 B(여유 시 데모).
**대신 미루는 기존 P1**: 이미지 업로드, 저장 기능. 게시판 읽기·쓰기와 생활정보 탐색은 코어 유지.

---

## 9. 롤백 분석 — 결론: 코드 롤백 없음 (2차 정정 포함)

기존 자산이 전부 새 방향의 토대다(독립 검토가 스키마·코드 대조로 전 항목 사실 확인):

| 기존 자산 | 새 방향에서의 역할 |
|---|---|
| `regions.centroid_lat/lng` (홍성 12) | 지도 묶음 핀 좌표 원천 |
| `life_info.latitude/longitude` | 실좌표 핀(검수 후) |
| 매칭(동의·상호성·시/군 스코프·RLS) | 이웃·호출의 권한 기반 |
| 게시판 CRUD / 생활정보+신선도 | 라우팅 ①④ / ②③의 목적지 |
| i18n·인증·프로필 | 그대로 |

- **필요한 DB 변경 (1차 초안의 "1건뿐"은 오류 — 정정)**: 데모 기준 **CHECK 2건** —
  ① `life_info` category에 'support' ② `posts` category에 'help'.
  v1.1의 presence는 `profiles.last_seen_at` 추가 + `neighbor_profiles` 뷰 재생성(**D-012 절차 필수**).
- **대체(롤백 아님)**: 홈 구성·하단 탭 배치.
- **앵커**: `v0.3.0`(0d463eb) 태그 푸시 완료. 실험 실패 시 main은 계속 안정 — 브랜치 폐기로 충분.

---

## 10. 미결정 — 아침 승인 필요 목록 (독립 검토 P1-5 반영 확장)

- [ ] **A. 개정 방향 승인** — §2 IA(5탭, 생활정보의 지도 흡수) + §8 축소안
- [ ] B. Leaflet 도입("새 라이브러리 금지" 명시 예외) + 타일 제공자
- [ ] C. 라우터 데모 범위 = 단계 A (LLM C/D는 v1.1 비용+동의 별도)
- [ ] D. DB 변경 2건(support·help CHECK) 승인
- [ ] E. 상담기관 검수 책임·방법 (검수 전 실기관 연락처 미노출 원칙 유지)
- [ ] F. presence v1.1 연기 승인 (도입 조건: 별도 플래그·절사·D-012 절차 — §5)
- [ ] G. 사용성 테스트 구성(§7 정량 기준·저문해력/아동 참가자)
- [ ] H. 이웃·게시글 지도 핀의 v1.1 도입 조건(최소 임계 N≥3, 로그인 한정, v1.3 §3.3 개정) 승인
- [ ] I. **만 14세 미만 정책**: 아동 테스터 보호자 동의 절차 / 실가입 아동의 법정대리인 동의(개인정보
      보호법) — "10세도 쓸 수 있게"가 "10세가 가입 대상"인지 여부 결정
- [ ] J. Web Speech 제3자 전송 고지 문구 + **개인정보 처리방침 초안 작성 시점**(v1.3 §9의 기존 의무)
- [ ] K. Week 3 이월 요건(동의 문구 확대 고지) 처리 시점 — v1.5 작업에 포함 여부
- [ ] L. v1.3 동반 개정 확정: §5 "지도는 P1·핵심 내비 아님" 반전 / §3.1 P0-8 "목록" 문구 / §3.3
      "사용자 밀도" 제외(H와 연동) / §2.2 성공 기준·60초 과업 재정의(타일 로딩 포함) / §10 축소
      순서에서 지도 위치 / v1.4 §3 "지도 핀 연기"의 부분 부활

승인 후 진행 순서: D(마이그레이션) → §8의 1→2→3→4, 각 단계는 기존 절차(한 작업·계획 승인·
독립 재검수) 유지.

---

## 11. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.4 | 2026-07-28 | 이웃 매칭(목록형) 재도입 |
| v1.5 1차 | 2026-07-29 | 지도 홈·직관 우선·고민 라우터·이웃 호출 초안 |
| **v1.5 2차** | 2026-07-29 | 독립 검토 반영: DB 변경 목록 정정(P0-1), 데모 지도에서 인적 핀 제외(P0-2), 단계 A 안전장치 명세(P0-3), presence v1.1 연기, Web Speech 제3자 전송 고지, 미결정 12항목 확장. **승인 대기** |
