# 농사다마 PRD v1.5 — 지도·음성 중심 직관 UX 개정안 (초안)

**작성일**: 2026-07-29 (야간 자율 작성)
**문서 상태**: ⚠️ **승인 대기 초안** — 제품 오너 결정 전까지 구현하지 않는다 (PRD 11.7 변경 통제)
**기준 문서**: PRD_v1_3.md(운영 원칙·품질 기준 유지) + PRD_v1_4.md(매칭 재도입) 위의 증분 개정
**롤백 앵커**: `v0.3.0` (커밋 0d463eb — 독립 재검수 통과 상태. §9 참조)

---

## 0. 이번 개정의 핵심 결정 (제품 오너 지시 요약)

> "지도로 보고, 텍스트 부담 없이, 포켓몬고처럼 직관으로. **10세도 쓸 수 있게.**
> 음성으로 고민을 말하면 분석해서 ① 쉬운 건 게시판 ② 만나서 해결할 건 생활정보
> ③ 어려운 건 LLM+상담기관 ④ 가벼운 친목은 근처 active 이웃 호출로 연결."

이 지시를 4주 데모의 현실(잔여 ~1주, 무료 플랜, 1인 개발)과 취약 사용자 안전 원칙 위에서
**단계화**한 것이 이 문서다. 전부 한 번에 구현하지 않는다.

### 유지되는 불변 원칙 (v1.3 §9 · v1.4 §4)
1. 정확한 사용자 위치를 수집·저장·공개하지 않는다 — 지도에서도 **사용자·게시글 핀은 읍·면 중심좌표만**
2. 모든 노출은 opt-in + RLS로 강제
3. 국가·언어·작목 비하드코딩
4. 무료 플랜 범위 (유료 API는 명시 승인 전 금지)
5. **음성 원본은 저장하지 않는다** (신규 — §3.4)

---

## 1. 비전 개정 — "직관 우선(Intuition-First)" 원칙

포켓몬고 레퍼런스의 본질은 게임이 아니라 **"지도가 곧 세계이고, 큰 버튼 하나로 행동한다"**는 것.

**10세 사용성 원칙** (모든 신규 화면의 완료 조건에 추가):
- 화면당 핵심 행동 1개, 큰 터치 대상(기존 44px → 주요 행동 56px+)
- 아이콘·이모지 우선, 텍스트는 보조 라벨 (번역 키 체계는 유지)
- 읽지 않아도 진행 가능: 색·아이콘·위치로 의미 전달
- 음성 입력/음성 안내(TTS)를 텍스트의 대체 경로로 (단계 도입)
- 실패해도 안전: 뒤로 가기 항상 명확, 파괴적 행동은 2단계 확인(기존 유지)

---

## 2. 새 정보구조(IA) — 메뉴 구성과 하위 구성

### 2.1 현행 vs 개정

| | 현행 (v1.4) | 개정 (v1.5) |
|---|---|---|
| 홈 | 이웃 섹션+바로가기 | **지도가 곧 홈** |
| 하단 탭 | 홈·게시판·생활정보·내정보 (+이웃 진입) | **지도 · 게시판 · 🎤말하기(중앙 강조) · 이웃 · 내정보** |
| 생활정보 | 목록 탭 | 지도 핀 + 목록은 지도 하단 시트로 통합 |
| 진입 흐름 | 랜딩→언어·지역 선택→홈 | 동일 (유지) |

### 2.2 개정 메뉴 트리

```text
지도 (홈, 비로그인 열람 가능)
├─ 핀: 생활정보 🏥🛒🏛🚌ℹ️ (좌표 있으면 실좌표, 없으면 읍·면 중심 묶음 핀)
├─ 핀: 게시글 💬 (읍·면 중심좌표에 개수 묶음 — 개별 위치 없음)
├─ 핀: 이웃 👥 (동의자만, 읍·면 중심 묶음 "N명" — 개인 핀 없음)
├─ 상단: 카테고리 아이콘 필터 바 (전체/병원/마트/행정/교통/기타/상담🆘)
└─ 핀 탭 → 하단 시트(요약: 아이콘+이름+신선도 배지) → 상세(기존 화면 재사용)

🎤 말하기 (중앙 큰 버튼 — 고민 라우터, §3)
├─ 1단계: 큰 아이콘 4택 (또는 음성으로 말하기)
│   💬 물어보고 싶어요 / 📍 가야 할 곳을 찾아요 / 🆘 어려운 일이 있어요 / ☕ 이웃과 얘기하고 싶어요
├─ 음성 입력(브라우저 STT, 모국어) → 텍스트 확인 → 분류
└─ 결과: ① 게시판 글쓰기(내용 프리필) ② 생활정보 지도/목록 ③ 상담기관 카드 ④ 이웃 호출

게시판 (유지 + 지도·라우터 진입점, 도움요청 카테고리 추가)
이웃 (유지 + "지금 활동 중" 표시 + 호출 응답, §5)
내 정보 (유지: 프로필·매칭 동의·언어/지역)
```

- 생활정보 **탭이 사라지는 것이 아니라 지도에 흡수**된다. 목록 뷰는 지도 하단 시트의
  "목록으로 보기"로 접근 가능(저사양·지도 로딩 실패 폴백 겸용).
- 랜딩·언어/지역 선택·로그인·프로필은 변경 없음.

### 2.3 이 단계에서 체크해야 할 것 (제품 오너 확인 목록)

- [ ] 하단 탭 5개(중앙 말하기) 구성 승인 여부 — 대안: 말하기를 지도 위 플로팅 버튼으로 두고 4탭 유지
- [ ] 생활정보 탭 → 지도 흡수 승인 여부 (사용성 테스트 과업 문구도 바뀜)
- [ ] 게시글·이웃을 지도에 "읍·면 묶음 핀"으로만 표시(개인 위치 없음) 확인
- [ ] 상담(🆘) 카테고리를 생활정보 데이터로 다루는 방식(§6) 승인

---

## 3. 🎤 고민 라우터 — 4-way 분류 설계

### 3.1 분류 체계 (제품 오너 지시 그대로)

| 분류 | 신호 예 | 연결 |
|---|---|---|
| ① 쉬움 — 물어보면 됨 | 사용법, 소소한 질문 | 게시판 글쓰기 (음성 텍스트 프리필, 카테고리 자동) |
| ② 쉬움 — 만나서/가서 해결 | 병원, 장보기, 서류, 교통 | 생활정보 (해당 카테고리 지도/목록) |
| ③ 어려움 — 전문 도움 필요 | 임금, 계약, 산재, 폭력, 비자 | **상담기관 카드(§6) 최우선** + (후속) LLM 보조 |
| ④ 가벼움 — 사람이 그리움 | 심심함, 외로움, 친목 | 이웃 호출(§5) — LLM 대신 **사람**이 답 |

### 3.2 안전 원칙 (협상 불가)

- **위기 신호 우선**: 폭력·감금·임금체불·산재 등이 감지되면 다른 분류보다 ③을 우선 제시하고
  공공 핫라인을 항상 함께 표시한다. LLM이든 키워드든 분류가 애매하면 ③로 승격한다(과소분류 금지).
- **LLM은 상담이 아니라 안내 보조**다. 법률·의료 판단을 생성하지 않고, 검수된 기관으로
  연결하는 요약·통역 보조까지만. 이 경계를 UI 문구로 명시한다.
- **음성 원본 미저장**: STT는 기기/브라우저 처리, 서버에는 사용자가 확인한 텍스트만
  (그마저 게시글로 저장을 선택한 경우만). 라우팅 판단 로그는 익명 통계 외 저장 안 함.

### 3.3 단계화 (핵심 — 전부 한 번에 만들지 않는다)

| 단계 | 내용 | 비용/의존 | 목표 버전 |
|---|---|---|---|
| **A. 가이드 라우터** | 큰 아이콘 4택 + (지원 브라우저에서) Web Speech API 음성→텍스트, **분류는 사용자가 아이콘으로 직접** | 0원, 외부 API 없음 | **데모 (P0')** |
| B. 키워드 보조 분류 | 텍스트에서 다국어 키워드 사전으로 ①~④ 추천(사용자가 확정) | 0원 | 데모 여유 시 |
| C. LLM 분류 | LLM(예: Claude Haiku)이 분류+위기 신호 감지 | API 비용, 프라이버시 정책 필요 | v1.1 (승인 필요) |
| D. LLM 상담 보조 | ③에서 상황 요약→기관 연결문 생성(모국어) | 〃 + 기관 협의 | v1.1+ |

- Web Speech API는 무료·미저장이지만 **언어/기기별 품질 편차**가 커서, 데모의 성공 기준은
  "음성 인식률"이 아니라 "아이콘 4택만으로 10세/저문해력 사용자가 라우팅을 완주"로 둔다.
  음성은 되면 좋은 가산점(progressive enhancement).
- C·D는 무료 플랜 원칙의 예외라 **별도 비용 승인 없이는 착수 금지**.

---

## 4. 지도 설계

- **라이브러리**: Leaflet (v1.3 §7.6에서 P1로 예정했던 그대로. "새 라이브러리 금지"의 명시 예외
  승인 필요 — 미결정 §10). 타일: OSM 정책 준수 무료 제공자(Carto 등) 중 데모 트래픽 허용 확인 후 선택.
- **데이터 소스** (전부 기존 스키마 — 마이그레이션 불필요):
  - 생활정보: `life_info.latitude/longitude`(운영자 검수 실좌표) → 실좌표 핀.
    좌표 없으면(현 샘플 전부) 읍·면 중심 묶음 핀 + "위치 검수 전" 배지. **좌표를 지어내지 않는다.**
  - 게시글/이웃: `regions.centroid_*` 묶음 핀만. 개인·글 단위 좌표는 존재하지도 않게 유지.
- **성능(PRD 9)**: Leaflet ~42kB gz + 타일 지연 로드. 선행 조건으로 **코드 스플리팅**(기존 백로그
  P2-9)을 데모 P0'로 승격 — 지도 도입 시 첫 화면 JS가 더 커지는 것을 상쇄.
- **폴백**: 타일 로드 실패/저사양 → 기존 목록 뷰 자동 전환(현행 화면 재사용이라 위험 낮음).

---

## 5. 이웃 호출 — "LLM 대신 사람"

- **presence**: `profiles.last_seen_at`(opt-in, 앱 사용 시 갱신) 기반 "지금 활동 중"(최근 30분).
  위치가 아니라 **활동 시각**이다. 매칭 동의자에게만, 같은 시/군 상호성 규칙 그대로.
- **데모 구현(P0' 후보)**: 호출 = `category='help'` 게시글 자동 생성(라우터 ④에서 프리필)
  + 이웃 목록에 "🟢 활동 중" 배지. 실시간 알림 없음 — 이웃이 게시판/홈에서 발견.
- **v1.1**: Supabase Realtime(무료 범위 확인 후)으로 활동 중 이웃에게 인앱 알림. 푸시는 그 뒤.
- 안전: 호출 글에도 기존 게시판 규칙(신고·차단은 백로그) 적용. 1:1 강제 아님 — 응답은 공개 글 댓글이
  아닌 게시글 방식이므로 기존 구조 그대로.

---

## 6. 상담기관 데이터 (라우팅 ③)

- `life_info`에 카테고리 `'support'` 추가(CHECK 제약 마이그레이션 1건 — 유일한 DB 변경).
  기존 검수 원칙 그대로: **출처(source_url)+최종확인일(verified_at) 없는 실기관 정보를 싣지 않는다.**
- 제품 오너 지정 후보(운영자 검수 필수 — 명칭·관할·연락처·존속 여부 확인 후 입력):
  농촌인력인권센터 · 외국인노동자(지원)센터 · 외국인인권센터 계열. ⚠️ 일부 정부 지원 센터는
  최근 예산·조직 변동이 있어 **현재 운영 여부 확인이 특히 중요**하다.
- 공공 핫라인(전국 공통, 비교적 안정): 외국인종합안내센터 **1345**, 고용노동부 상담 **1350** —
  이것도 시딩 시 운영자 최종 확인 후 verified_at을 채워 입력.
- 지도에서 🆘 카테고리로 노출 + 라우터 ③의 카드로 노출(전화 걸기 버튼 대형).

---

## 7. 접근성 완료 조건 추가 (모든 신규 화면)

기존 v1.3 §11.4에 다음을 추가:
- 텍스트를 읽지 않고 아이콘·색·위치만으로 핵심 행동 완수 가능한가
- 주요 행동 버튼 56px+ / 그 외 44px+ 유지
- 사용성 테스트에 "10세 어린이 또는 한국어·영어 모두 미숙한 성인 1명 이상" 포함

---

## 8. 범위 재편성 — 넣으면 무엇을 빼는가 (v1.3 §11.7-5)

**데모 잔여 기간(약 1주) 기준 제안:**

| 순위 | P0' (데모에 추가) | 대신 미루는 것 |
|---|---|---|
| 1 | 지도 홈(생활정보 실좌표/묶음 핀 + 게시글·이웃 묶음 핀) + 코드 스플리팅 | 이미지 업로드(P1), 저장 기능(P1) |
| 2 | 말하기 라우터 **단계 A**(아이콘 4택 + 선택적 STT) | LLM 분류(C)·상담보조(D) → v1.1 |
| 3 | 상담 카테고리(support) + 검수 시딩 틀 | — |
| 4 | 이웃 호출(help 글 + 활동 중 배지) | Realtime 알림 → v1.1 |

- 기존 v1.3 축소 순서 원칙 유지: 게시판 읽기·쓰기와 생활정보 탐색(이제 지도 포함)은 제거 불가 코어.
- 일정이 밀리면 역순(4→3→2)으로 뺀다. 지도(1)는 이번 개정의 정체성이라 마지막까지 지킨다.

---

## 9. 롤백 분석 — 결론: 코드 롤백 없음

**검토 결과, 되돌릴 것이 없다.** 기존 자산이 전부 새 방향의 토대다:

| 기존 자산 | 새 방향에서의 역할 |
|---|---|
| `regions.centroid_lat/lng` (홍성 12개) | 지도 묶음 핀·거리의 좌표 원천 |
| `life_info.latitude/longitude` 컬럼 | 실좌표 핀(운영자 검수 후) |
| 매칭(동의·상호성·시/군 스코프·RLS) | 이웃 핀·호출 기능의 권한 기반 |
| 게시판 CRUD | 라우팅 ①·④의 목적지 |
| 생활정보+신선도 배지 | 라우팅 ②·③의 목적지 |
| i18n·인증·프로필 | 그대로 |

- **대체(롤백 아님)되는 것**: 홈 화면 구성과 하단 탭 배치 — 화면 계층만 교체.
- **롤백 앵커**: `v0.3.0` 태그(0d463eb, 독립 재검수 통과 상태) 생성·푸시 완료.
  새 방향은 기능 브랜치에서 진행하고, 실험이 실패하면 `git reset --hard v0.3.0`이 아니라
  **브랜치 폐기**로 충분하다(main은 계속 안정). DB는 신규 마이그레이션이 'support' 카테고리
  1건뿐이라 되돌릴 부담이 사실상 없다.

---

## 10. 미결정 — 아침 승인 필요 목록

- [ ] **A. 이 개정안(v1.5) 방향 승인** — 특히 §2 IA(5탭·생활정보의 지도 흡수)
- [ ] B. Leaflet 도입 승인("새 라이브러리 금지"의 명시 예외) + 타일 제공자
- [ ] C. 말하기 라우터 데모 범위 = 단계 A 확정 (LLM C·D는 v1.1 비용 승인 별도)
- [ ] D. §8 범위 재편성(P0' 4건과 미루는 항목) 승인
- [ ] E. 상담기관 시딩: 운영자 검수 책임자·확인 방법 (실기관 연락처는 검수 전 미노출 원칙)
- [ ] F. presence(last_seen_at) 컬럼 추가 승인 — opt-in 문구 포함
- [ ] G. 사용성 테스트 대상에 10세/저문해력 사용자 포함 여부

승인되면: A→(마이그레이션 support+last_seen)→지도→라우터A→호출 순으로 기존 절차
(한 작업·계획 승인·독립 재검수)대로 진행한다.

---

## 11. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.4 | 2026-07-28 | 이웃 매칭(목록형) 재도입 |
| **v1.5(초안)** | 2026-07-29 | 지도 홈·직관 우선(10세 사용성)·🎤 고민 라우터(4-way, 단계화)·이웃 호출·상담기관 카테고리. 롤백 앵커 v0.3.0. **승인 대기** |
