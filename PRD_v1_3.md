# 농사다마 (NongsaDama) PRD v1.3

**작성일**: 2026-07-26  
**작성자**: Radlearner  
**문서 목적**: 4주간 1인 바이브코딩으로 무료 데모를 완성하기 위한 제품·개발 기준 문서  
**기술 방향**: React + Supabase + Vercel  
**문서 상태**: Cowork 반복 보완용 초안  
**제품 원칙**: 특정 국적·언어·메신저에 종속되지 않는 구조

---

## 0. 이번 버전의 의사결정

농사다마는 농촌에서 생활하는 외국인 계절근로자가 **자신의 언어로 지역 생활정보와 지역 게시글을 찾는 모바일 우선 웹앱**이다.

기존 초안의 “내 주변 같은 국적 사용자 찾기”는 개인정보·안전·콜드스타트 위험이 크므로 4주 데모에서는 제외한다. 대신 **읍면동 단위 지역 커뮤니티**를 핵심으로 검증한다.

베트남·베트남어·Zalo 등은 사용자 조사와 화면 검증을 위한 하나의 사례일 뿐 제품의 기본값이 아니다. 국가, 언어, 인증 제공자는 코드에 고정하지 않고 설정과 데이터로 추가할 수 있어야 한다.

### 4주 데모에서 반드시 증명할 것

> 외국인 근로자가 언어와 지역을 선택한 뒤, 자기 지역의 유용한 생활정보와 게시글을 쉽게 찾고 직접 글을 남길 수 있는가?

### 이번 데모가 증명하지 않는 것

- 실시간 사용자 위치 공유
- 친구 찾기 또는 사용자 간 DM
- 구인·구직 및 고용 중개
- 모든 언어를 대상으로 한 완전 자동번역
- 모든 국가의 메신저 로그인
- 전국 단위 서비스
- 지자체용 관리자 대시보드
- 수익화

---

## 1. 문제와 대상 사용자

### 1.1 핵심 문제

1. 농촌 지역의 외국인 근로자는 병원, 마트, 행정기관, 교통 등 생활정보를 자기 언어로 찾기 어렵다.
2. 지역 정보가 페이스북, 단체 채팅방, 구두 전달 등에 흩어져 있어 다시 찾기 어렵다.
3. 정확한 위치를 공개하면서 낯선 사용자를 연결하면 사생활과 안전 문제가 생길 수 있다.

### 1.2 1차 사용자

**외국인 계절근로자 A**

- 스마트폰으로 QR 링크에 접속한다.
- 한국어가 익숙하지 않다.
- 국적과 사용하는 언어는 특정하지 않는다.
- 현재 일하는 시군 또는 읍면동의 생활정보가 필요하다.
- 앱 설치와 복잡한 회원가입을 부담스러워한다.

### 1.3 2차 이해관계자

- 외국인 근로자를 고용한 농가주
- 계절근로 담당 지자체와 지원기관
- 농협, 농업기술센터 등 현장 접점 기관

2차 이해관계자는 데모의 직접 사용자가 아니라 **배포 채널과 인터뷰 대상**이다.

---

## 2. 제품 가설과 성공 기준

### 2.1 제품 가설

> 특정 1개 시군에서 검증 대상 언어를 사용하는 외국인 근로자에게 지역 생활정보와 지역 게시판을 제공하면, 사용자는 필요한 정보를 열람하고 다시 방문하거나 직접 글을 작성한다.

첫 검증에서 한 언어를 선택하는 것은 개발 범위를 제한하기 위한 것이며 서비스 대상을 해당 국적으로 제한한다는 뜻이 아니다.

### 2.2 4주 데모 성공 기준

실사용자가 충분하지 않은 초기 단계이므로 허수성 가입자 목표보다 과업 완료와 인터뷰 반응을 우선한다.

| 구분 | 목표 |
| --- | --- |
| 인터뷰 | 외국인 근로자 2명 이상, 농가주 또는 현장 관계자 2명 이상 |
| 사용성 테스트 | 5명 중 4명 이상이 도움 없이 지역 선택 → 정보 열람 완료 |
| 작성 과업 | 5명 중 3명 이상이 도움 없이 게시글 작성 완료 |
| 속도 | 첫 유용한 정보 확인까지 60초 이내 |
| 품질 | 모바일 주요 흐름의 치명적 오류 0건 |
| 의향 | 테스트 사용자 50% 이상이 “다시 쓰고 싶다” 응답 |

장기 KPI인 WAU, 4주 리텐션, 지자체 미팅 수는 파일럿 이후에 측정한다.

---

## 3. MVP 범위

### 3.1 P0: 4주 안에 반드시 구현

1. 모바일 랜딩
2. 지원 언어 목록에서 언어 선택
3. 지역 선택: 데모 대상 1개 시군과 하위 읍면동
4. 로그인: 기본 인증 1종과 향후 메신저 인증을 수용하는 구조
5. 지역 게시글 목록
6. 게시글 상세 보기
7. 게시글 작성·수정·삭제
8. 생활정보 목록과 상세 보기
9. 카테고리 필터: 병원, 마트, 행정, 교통, 기타
10. 내 프로필의 언어·지역 변경
11. 운영자가 Supabase에서 시딩 데이터 관리

### 3.2 P1: P0 완료 후 여유가 있을 때

- 게시글 및 생활정보 저장
- 사진 1장 업로드
- 지도에서 생활정보 핀 보기
- 부적절한 게시글 신고
- PWA 설치 지원

### 3.3 이번 버전에서 제외

- 사용자 위치 또는 사용자 밀도 표시
- 실시간 채팅, DM, 푸시 알림
- 소셜 로그인
- 사용자 게시글 자동번역 API
- 국가별 메신저 로그인 다중 구현
- 댓글, 좋아요, 팔로우
- 공공데이터 API 실시간 연동
- 관리자 전용 웹 화면
- 분석 대시보드
- 결제, 광고, 구인·구직

---

## 4. 핵심 사용자 흐름

### 4.1 첫 방문

1. 현장의 QR 코드를 스캔한다.
2. 자신이 사용할 언어를 선택한다.
3. 일하는 지역을 직접 선택한다. 위치 권한에 동의한 경우 가까운 지역을 추천할 수 있다.
4. 해당 지역의 최신 게시글과 생활정보를 바로 본다.
5. 글을 작성하려 할 때만 로그인한다.

**원칙**: 읽기에는 로그인을 요구하지 않는다. 쓰기와 내 정보 관리에만 로그인을 요구한다.

국적과 지역은 메신저 계정만으로 확인할 수 없으므로 “자동인증”이라고 부르지 않는다. 국가는 선택 입력, 지역은 사용자 선택 또는 일회성 위치 동의에 따른 추천값으로 취급한다.

### 4.2 게시글 작성

1. 지역 피드에서 글쓰기 버튼을 누른다.
2. 로그인하지 않았다면 현재 지원하는 인증 방식으로 로그인한다.
3. 카테고리, 제목, 본문을 입력한다.
4. 게시 후 자신의 지역 피드에서 결과를 확인한다.

### 4.3 생활정보 찾기

1. 생활정보 탭에 들어간다.
2. 병원 등 카테고리를 선택한다.
3. 주소, 운영시간, 전화번호, 언어지원 여부를 확인한다.
4. 전화 걸기 또는 지도 앱 열기 링크를 누른다.

---

## 5. 정보구조와 화면

```text
비로그인/공통
├─ 랜딩
├─ 언어·지역 선택
├─ 홈
├─ 지역 게시판
│  └─ 게시글 상세
└─ 생활정보
   └─ 생활정보 상세

로그인 필요
├─ 게시글 작성/수정
└─ 내 정보
```

### 하단 내비게이션

- 홈
- 게시판
- 생활정보
- 내 정보

지도는 P1이며 핵심 내비게이션으로 두지 않는다.

---

## 6. 콘텐츠와 다국어 정책

### 6.1 UI 번역

- React의 정적 번역 사전을 사용한다.
- MVP는 기본 언어와 검증 대상 언어를 각각 설정에서 지정한다.
- 모든 UI 문자열은 컴포넌트에 직접 쓰지 않고 번역 키로 관리한다.
- 언어 추가가 데이터베이스 변경 없이 가능해야 한다.
- 언어는 ISO 언어 코드 기반으로 관리하고 특정 코드(`vi`, `ko` 등)를 비즈니스 로직에 직접 넣지 않는다.

### 6.2 사용자 게시글

- 사용자가 입력한 원문을 그대로 저장하고 `source_locale`을 함께 저장한다.
- 데모에서는 자동번역을 제공하지 않는다.
- 필요하면 운영자가 검수한 번역문을 별도 필드에 추가할 수 있다.
- 외부 번역 API는 사용자 수요 검증 후 v1.1에서 결정한다.

### 6.3 생활정보

- 운영자가 검증 대상 언어의 제목과 설명을 직접 입력한 검수 콘텐츠를 사용한다.
- 출처와 최종 확인일을 표시한다.
- 전화번호와 운영시간은 오래될 수 있으므로 “방문 전 확인” 문구를 노출한다.

### 6.4 국가·언어 분리 원칙

- 국가는 `country_code`, 언어는 `locale`로 서로 분리한다.
- 한 국가가 여러 언어를 사용하거나 한 언어가 여러 국가에서 쓰이는 경우를 허용한다.
- 국적 입력은 선택 사항이며 커뮤니티 접근 권한을 제한하는 수단으로 사용하지 않는다.
- 지역 피드는 국적이 아니라 사용자가 선택한 지역을 기준으로 한다.
- UI 언어와 게시글 원문 언어는 서로 다를 수 있다.

---

## 7. 무료 데모 아키텍처

### 7.1 구성

```text
사용자 모바일 브라우저
        │
        ▼
React + TypeScript + Vite
Vercel 정적 배포
        │
        ▼
Supabase
├─ Auth: 기본 인증 + 교체 가능한 외부 인증 연결
├─ Postgres: 프로필, 지역, 게시글, 생활정보
├─ Storage: 선택적 이미지 업로드
└─ Row Level Security: 읽기/쓰기 권한 통제
```

### 7.2 프론트엔드

- React + TypeScript + Vite
- React Router
- TanStack Query: 서버 상태와 캐시
- React Hook Form + Zod: 입력과 검증
- CSS는 Tailwind CSS 또는 단일 UI 방식 하나만 선택
- 정적 UI 번역 사전
- 모바일 우선 반응형 디자인

상태관리 라이브러리는 별도로 추가하지 않는다. 인증 상태와 간단한 UI 상태는 React Context로 충분하다.

### 7.3 백엔드

- 별도 Node 서버 없이 Supabase 클라이언트로 접근
- 모든 공개 테이블에 Row Level Security 적용
- 데이터 검증에 데이터베이스 제약조건 사용
- 서비스 역할 키는 브라우저와 저장소에 절대 포함하지 않음
- 운영 데이터 입력은 초기에는 Supabase Dashboard 사용

### 7.4 인증 전략

QR 코드는 앱으로 들어오는 링크이며 그 자체가 신원 인증 수단은 아니다.

#### MVP 기본 흐름

```text
QR 접속
→ 언어 선택
→ 지역 직접 선택 또는 위치 동의 후 추천
→ 게시글·생활정보 열람
→ 글 작성 시 로그인
→ 선택한 언어·지역을 프로필에 저장
```

- 읽기 전에 회원가입을 강제하지 않는다.
- MVP 인증 방식은 실제 테스트 대상의 접근성을 확인해 한 종류만 선택한다.
- 이메일 사용률이 낮다면 매직링크를 기본값으로 확정하지 않는다.
- SMS OTP는 발송 비용과 국가별 수신 문제를 확인하기 전 무료 데모의 기본값으로 사용하지 않는다.

#### 본국 메신저 로그인

- 메신저 로그인은 `auth_provider` 어댑터로 분리한다.
- 공식 OAuth 또는 OpenID Connect를 제공하고 대상 사용자가 실제로 많이 쓰는 경우에만 하나씩 추가한다.
- 메신저별 앱 등록, 심사, 콜백 처리, 개인정보 정책이 다르므로 “모든 메신저 공통 로그인”으로 구현하지 않는다.
- 메신저에서 받은 외부 사용자 ID는 내부 사용자 ID와 분리해 저장한다.
- 메신저 프로필로 국적이나 현재 지역을 추정하거나 자동 확정하지 않는다.
- 공식 로그인 API가 없는 메신저는 비공식 자동화 방식으로 연결하지 않는다.

#### 인증 실험의 승인 조건

1. 인터뷰 대상의 해당 메신저 사용률이 높다.
2. 공식 웹 로그인 API가 존재한다.
3. 개발자 앱 등록과 운영 정책을 충족할 수 있다.
4. Supabase와의 연결 방식 및 로그아웃·탈퇴 처리가 검증됐다.
5. 기본 인증 없이도 계정 복구가 가능한지 확인됐다.

### 7.5 배포

- GitHub 저장소의 `main` 브랜치를 Vercel 프로덕션에 연결
- 기능 개발은 별도 브랜치에서 수행
- Pull Request마다 Vercel 미리보기 배포 사용
- Supabase 프로젝트는 데모용 1개로 시작
- 환경변수는 `.env.local`과 Vercel 설정에서 관리
- `.env*` 및 비밀키는 Git에 커밋하지 않음

### 7.6 지도

P1에서만 Leaflet 기반 지도를 검토한다. 데모 지도에는 생활정보 위치만 표시하며 사용자의 위치는 저장하거나 공개하지 않는다. 무료 지도 타일은 제공자의 사용 정책과 호출량 제한을 확인한 뒤 선택한다.

### 7.7 비용 원칙

- Vercel과 Supabase 무료 플랜 범위에서 동작하도록 설계
- 유료 번역·SMS·지도 API는 사용하지 않음
- 이미지 크기와 업로드 수 제한
- 외부 API가 없어도 시딩 데이터만으로 전체 데모가 동작해야 함
- 무료 플랜 한도와 정책은 구현 시작 시 공식 문서에서 다시 확인

---

## 8. 데이터 모델

### 8.1 테이블

#### `regions`

| 필드 | 설명 |
| --- | --- |
| id | UUID |
| parent_id | 상위 지역, nullable |
| names | locale을 키로 사용하는 지역명 JSON |
| level | city 또는 town |
| is_active | 데모 노출 여부 |

#### `profiles`

| 필드 | 설명 |
| --- | --- |
| id | Supabase Auth 사용자 ID |
| nickname | 공개 닉네임 |
| preferred_locale | 사용자가 선택한 UI 언어 코드 |
| country_code | 선택 입력한 국가 코드, nullable |
| region_id | 선택 지역 |
| auth_provider | 현재 연결된 인증 제공자 |
| role | user 또는 admin |
| created_at | 생성 시각 |

국가 정보는 선택 입력으로만 받고 이를 “국적 인증”으로 표현하지 않는다. 비자 유형, 정확한 주소, 현재 좌표는 MVP에서 수집하지 않는다.

#### `posts`

| 필드 | 설명 |
| --- | --- |
| id | UUID |
| author_id | 작성자 |
| region_id | 게시 지역 |
| category | question, info, daily, other |
| title | 제목 |
| body | 본문 |
| source_locale | 원문 언어 코드 |
| status | published, hidden, deleted |
| created_at / updated_at | 생성·수정 시각 |

#### `life_info`

| 필드 | 설명 |
| --- | --- |
| id | UUID |
| region_id | 지역 |
| category | hospital, market, government, transport, other |
| localized_content | locale별 명칭과 설명 JSON |
| address | 주소 |
| phone | 전화번호 |
| latitude / longitude | 장소 좌표, nullable |
| source_url | 출처 |
| verified_at | 최종 확인일 |
| is_published | 공개 여부 |

#### P1 선택 테이블

- `bookmarks`
- `reports`
- Storage의 `post-images` 버킷

### 8.2 권한 정책

- 누구나 공개 상태의 게시글과 생활정보를 읽을 수 있다.
- 로그인 사용자는 자신의 프로필과 게시글만 생성·수정할 수 있다.
- 사용자는 자신의 게시글만 삭제할 수 있다.
- `admin`만 생활정보를 생성·수정·비공개 처리할 수 있다.
- UI에서 숨기는 것으로 권한을 대신하지 않고 RLS로 강제한다.

---

## 9. 비기능 요구사항

### 개인정보와 안전

- 정확한 사용자 위치를 수집·저장·공개하지 않는다.
- 공개 프로필에 실명, 전화번호, 농장명, 숙소 위치를 요구하지 않는다.
- 삭제는 화면에서 즉시 사라지고 데이터 정책에 맞춰 처리한다.
- 개인정보 처리방침과 이용 안내의 최소 초안을 배포 전 추가한다.

### 성능과 접근성

- 보급형 안드로이드와 느린 모바일 네트워크를 기준으로 한다.
- 첫 화면의 이미지와 JavaScript 크기를 최소화한다.
- 버튼 터치 영역, 글자 크기, 색 대비를 모바일 기준으로 점검한다.
- 빈 상태, 로딩, 오류, 재시도 화면을 모든 데이터 화면에 둔다.

### 품질

- TypeScript 오류 0건
- 프로덕션 빌드 성공
- 핵심 흐름 자동 테스트 또는 재현 가능한 수동 체크리스트 보유
- 모바일 화면 360px 기준 깨짐 없음
- RLS 권한 테스트 통과

---

## 10. 4주 실행 계획

### Week 1: 방향 확정과 기반

- 외국인 근로자·농가주 인터뷰
- 대상 지역 1곳과 언어 1개 확정
- 와이어프레임과 화면별 완료 조건 확정
- React, Supabase, Vercel 기본 연결
- 데이터 모델과 RLS 작성
- GitHub 및 버전 관리 규칙 설정
- 인증 방식 사용자 조사와 기술 스파이크

**완료 기준**: 배포된 빈 앱에서 언어·지역 선택과 데이터 읽기가 가능하다.

### Week 2: 읽기 경험 완성

- 랜딩, 홈, 게시판 목록·상세
- 생활정보 목록·상세
- 다국어 UI
- 시딩 데이터 20~30건
- 모바일 UI 점검

**완료 기준**: 로그인 없이 지역 정보와 게시글을 처음부터 끝까지 탐색할 수 있다.

### Week 3: 사용자 작성과 권한

- 확정된 기본 로그인 1종
- 프로필
- 게시글 작성·수정·삭제
- RLS 권한 테스트
- 오류·로딩·빈 상태

**완료 기준**: 서로 다른 두 계정에서 타인의 글을 수정할 수 없고 자신의 글만 관리할 수 있다.

### Week 4: 검증과 데모

- 실제 사용자 사용성 테스트
- 발견된 P0 버그 수정
- 분석 이벤트 최소 적용
- QR 진입과 Vercel 프로덕션 배포
- README, 데모 계정, 운영 체크리스트 작성
- 데모 영상 또는 화면 캡처 준비

**완료 기준**: 새 사용자가 QR 접속 후 60초 이내에 지역 정보를 찾고, 로그인 후 글을 작성할 수 있다.

### 범위 축소 순서

일정이 밀리면 다음 순서로 제거한다.

1. 지도
2. 이미지 업로드
3. 저장 기능
4. 신고 기능
5. PWA 설치

게시판 읽기·쓰기와 생활정보 탐색은 제거하지 않는다.

---

## 11. 버전 및 작업 관리 지침

### 11.1 버전 의미

| 버전 | 의미 |
| --- | --- |
| v0.1.0 | 화면 골격과 배포 연결 |
| v0.2.0 | 읽기 전용 데모 완성 |
| v0.3.0 | 로그인과 게시글 작성 완성 |
| v0.4.0 | 테스트 가능한 릴리스 후보 |
| v1.0.0 | 4주 데모 확정본 |
| v1.0.1 | 기능 변화 없는 버그 수정 |
| v1.1.0 | 검증 후 승인된 기능 추가 |

### 11.2 Git 규칙

- `main`: 언제나 데모 가능한 안정 버전
- 기능 브랜치: `feat/기능명`
- 버그 브랜치: `fix/문제명`
- 문서 브랜치: `docs/문서명`
- 한 작업은 한 브랜치와 한 Pull Request로 관리
- 검증 전 코드를 `main`에 직접 반영하지 않음
- 안정 마일스톤마다 `v0.x.0` 태그 생성
- 긴급 수정은 마지막 정상 태그에서 새 브랜치를 만들어 처리

### 11.3 커밋 원칙

- 한 커밋에는 한 가지 목적만 담는다.
- 예: `feat: 지역별 게시글 목록 추가`
- 예: `fix: 비로그인 사용자의 글쓰기 접근 차단`
- 자동 생성 파일, 비밀키, 불필요한 대용량 파일은 커밋하지 않는다.

### 11.4 각 기능의 완료 정의

기능은 아래 조건을 모두 만족해야 완료다.

1. PRD의 완료 조건 충족
2. 모바일 화면 확인
3. 정상·빈 상태·오류 상태 확인
4. 권한과 입력 검증 확인
5. TypeScript 검사와 프로덕션 빌드 통과
6. Vercel 미리보기에서 수동 검증
7. 변경 내용과 테스트 결과를 Pull Request에 기록
8. 사용자가 확인한 뒤 `main`에 반영

### 11.5 구현 후 독립 재검수

모든 코드 작업은 구현과 재검수를 분리한 이중 확인 절차를 거친다.

```text
요구사항 확정
→ 구현 담당 작업
→ 자동 검사와 빌드
→ 새로운 대화/에이전트의 독립 코드 리뷰
→ 발견 사항 수정
→ 동일 항목 재검증
→ 사용자 승인
→ main 반영 및 버전 태그
```

- 최초 구현 대화가 자기 결과를 승인하지 않는다.
- 재검수자는 PRD, 변경 diff, 테스트 결과만 보고 요구사항 누락·회귀·보안·RLS·오류 처리를 점검한다.
- 재검수자에게 새 기능 구현을 함께 요청하지 않는다.
- P0 또는 P1 지적이 남아 있으면 병합하거나 완료 버전을 올리지 않는다.
- 수정 후 최초 검수 항목을 다시 실행하여 해결 여부를 기록한다.
- 리팩터링도 기능 작업과 분리하며, 동작 변경이 없음을 테스트로 입증한다.

### 11.6 버그 기록 형식

```text
[BUG]
발견 버전:
발생 화면:
재현 절차:
기대 결과:
실제 결과:
심각도: P0 / P1 / P2
관련 화면 또는 로그:
수정 버전:
검증 결과:
```

- P0: 핵심 흐름 불가, 데이터·권한 문제 → 즉시 수정
- P1: 주요 기능 오류이나 우회 가능 → 현재 마일스톤에서 수정
- P2: 문구·정렬 등 경미한 문제 → 백로그

### 11.7 변경 통제

새 기능 요청이 생기면 바로 코딩하지 않고 다음을 먼저 기록한다.

1. 해결하려는 사용자 문제
2. 대상 사용자
3. 성공 기준
4. 기존 기능에 미치는 영향
5. 이번 4주에 넣을 경우 제거할 기능
6. 반영할 목표 버전

P0 범위를 바꾸면 PRD 버전을 올리고 변경 이력을 남긴다.

---

## 12. Claude Code 작업 지침

Claude Code에는 전체 앱을 한 번에 요청하지 않고 아래 순서로 한 작업씩 요청한다. 구현 세션과 검수 세션은 분리한다.

1. 현재 PRD와 저장소 구조 읽기
2. 구현 대상 한 가지와 완료 조건 확인
3. 수정 계획과 예상 파일 목록 제시
4. 사용자 승인 후 구현
5. 검사·빌드·관련 테스트 실행
6. 변경 파일, 검증 결과, 남은 위험 보고
7. 정상 확인 후 커밋 후보 메시지 제안
8. 별도의 새 대화 또는 리뷰 에이전트에 독립 재검수 요청
9. 지적 수정 후 전체 검증을 다시 실행

### 매 작업 공통 프롬프트

```text
PRD_v1_3.md를 제품 요구사항의 기준으로 사용해라.
이번 작업 범위는 [기능 한 가지]뿐이다.
먼저 관련 파일을 읽고 구현 계획, 수정 예상 파일, 데이터베이스 영향,
보안/RLS 영향, 테스트 방법을 제시해라. 승인 전에는 코드를 수정하지 마라.

구현 후에는 TypeScript 검사, 프로덕션 빌드, 관련 테스트를 실행하고
변경 파일, 완료 조건 충족 여부, 남은 위험, 권장 커밋 메시지를 보고해라.
요청 범위 밖 리팩터링과 새 라이브러리 추가는 하지 마라.
기존 정상 기능을 깨뜨릴 가능성이 있으면 작업 전에 알려라.
```

### 독립 재검수 프롬프트

```text
당신은 이 변경을 구현한 담당자가 아닌 독립 코드 리뷰어다.
PRD_v1_3.md의 이번 기능 요구사항, 변경 diff, 테스트 결과를 검토해라.
새 기능을 추가하거나 코드를 바로 수정하지 마라.

다음을 우선 확인해라.
1. 요구사항과 완료 조건 누락
2. 기존 기능 회귀 가능성
3. 인증, 개인정보, Supabase RLS 취약점
4. 다국가·다언어 구조에 특정 국가나 언어가 하드코딩됐는지
5. 오류·로딩·빈 상태와 모바일 접근성
6. 테스트가 실제 실패 가능성을 검증하는지

각 발견 사항에 심각도(P0/P1/P2), 근거 파일과 위치, 재현 방법,
권장 수정 방향을 적어라. 문제가 없으면 확인한 범위와 남은 위험을 명시해라.
```

### 프로젝트 루트에 둘 문서

- `PRD_v1_3.md`: 제품 범위와 완료 기준
- `README.md`: 실행·환경변수·배포 방법
- `CHANGELOG.md`: 버전별 완료 기능과 수정 내역
- `CLAUDE.md`: Claude Code 공통 행동 지침
- `docs/TEST_CHECKLIST.md`: 구현 검사와 독립 재검수 결과
- `docs/DECISIONS.md`: 주요 기술·제품 의사결정과 이유

---

## 13. Cowork 보완 체크리스트

Cowork에서 PRD를 다시 보완할 때 다음 질문에 답하지 못하는 제안은 보류한다.

- 실제 인터뷰에서 확인된 문제인가?
- 1차 검증 지역과 검증 언어가 명확한가?
- 특정 국가·언어·메신저가 코드에 고정되지 않았는가?
- 4주 안에 혼자 구현하고 검증할 수 있는가?
- 새 기능이 핵심 과업 성공률을 높이는가?
- 개인정보 또는 안전 위험을 키우지 않는가?
- 무료 플랜 범위를 벗어나지 않는가?
- 성공 여부를 관찰하거나 측정할 수 있는가?
- 추가한다면 무엇을 뺄 것인가?

---

## 14. 출시 전 결정이 필요한 항목

- [ ] 1차 파일럿 시군과 읍면동
- [ ] 첫 검증 언어와 번역 검수자 또는 검수 방법
- [ ] 인터뷰 대상이 실제로 사용하는 로그인 수단
- [ ] 기본 인증 방식 1종
- [ ] 메신저 로그인 기술 스파이크 포함 여부
- [ ] 서비스 공개 명칭과 도메인
- [ ] 개인정보 처리방침의 주체와 문의처
- [ ] 시딩 생활정보의 출처와 최종 확인일
- [ ] 분석 도구 사용 여부와 개인정보 동의 범위
- [ ] 지도 P1 포함 여부

---

## 15. 변경 이력

| 버전 | 날짜 | 변경 내용 |
| --- | --- | --- |
| v1.0 | 2026-07-22 | 최초 아이디어, 시장, IA, 수익화 초안 |
| v1.2 | 2026-07-26 | 4주 1인 무료 데모에 맞게 범위 축소, 개인정보 보호 중심 재설계, React·Supabase·Vercel 아키텍처와 버전 관리 지침 추가 |
| v1.3 | 2026-07-26 | 특정 국가·언어 종속 제거, 국가·언어 분리 모델, 단계형 메신저 인증 전략, 구현·독립 재검수 이중 확인 절차 추가 |
