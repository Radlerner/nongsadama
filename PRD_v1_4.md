# 농사다마 PRD v1.4 — 이웃 연결(매칭) 재도입 개정

**작성일**: 2026-07-26
**성격**: PRD_v1_3.md에 대한 **증분 개정**. 여기 명시된 변경 외에는 v1.3이 그대로 유효하다.
**개정 사유**: 제품 오너 결정 — 서비스의 원 취지(같은 언어를 쓰는 농촌 이웃 연결, v1.0 "동향 지도")가
v1.3 축소 과정에서 완전히 빠졌다. 안전·개인정보 원칙을 유지하는 형태로 재도입한다.

---

## 1. 변경 요약 (11.7 변경 통제 기록)

| 항목 | 내용 |
| --- | --- |
| 해결하려는 사용자 문제 | 바로 옆 마을에 같은 언어/국적 사람이 있어도 서로 존재를 모르는 고립 (v1.0 §3) |
| 대상 사용자 | 외국인 계절근로자 (1차), 언어·지역·작목이 같은 이웃을 찾고 싶은 사용자 |
| 성공 기준 | 사용성 테스트에서 "내 주변에 같은 언어 이웃이 있다"를 확인하는 과업 완료율, 이웃 목록 열람율 |
| 기존 기능 영향 | 프로필 확장(작목·공개 동의), 게시글 카드에 거리·국적 표시 추가. 기존 읽기 흐름 유지 |
| 추가 대신 제거 | **P1 전체 연기**(지도 핀, 이미지 업로드, 저장, 신고, PWA). 게시글 작성·수정·삭제는 유지 |
| 목표 버전 | v0.3.0(로그인·작성)과 함께 Week 3에 구현, 데모(v1.0.0)에 포함 |
| 일정 위험 | P0 순증. Week 3~4 부하 증가를 P1 전면 연기로 상쇄. 밀리면 §5 축소 순서 적용 |

## 2. 재도입 형태: "이웃 목록형" (지도·정확위치·DM 없음)

v1.3이 매칭을 뺀 이유였던 위험에 다음 설계로 답한다.

| v1.3의 우려 | v1.4 설계 응답 |
| --- | --- |
| 개인정보·안전(정확 위치 노출) | **정확 위치를 수집·저장·표시하지 않는다(불변)**. 위치 단위는 읍·면. 거리는 읍·면 중심좌표 간 **근사값**("약 3km")만 표시 |
| 낯선 사용자 연결의 안전 문제 | 1:1 연락(DM)은 데모에 포함하지 않는다. 연결은 "이웃 목록 → 그 이웃의 공개 게시글 보기 → 게시판 댓글 없는 구조상 게시글로 소통" 경로 |
| 콜드스타트(빈 목록) | 데모 시딩 계정(명시 표기)으로 목록 데모 가능하게 함. 파일럿 1개 시군 집중 |
| 국적을 접근 제한에 쓰지 않는 원칙(6.4) | 유지. 국적·작목·이웃 노출은 전부 **선택(opt-in)**이며 필터일 뿐 접근 권한이 아니다 |

### 2.1 이웃 목록 기능 (신규 P0)

- 위치: 하단 탭 구조 유지, **홈 화면에 "내 이웃" 섹션** + 상세 목록 화면 `/neighbors`
- 내용: 같은 시/군 내에서 매칭 공개에 동의한 사용자를 **읍·면 단위**로 목록 표시
  - 표시 항목: 닉네임, 읍·면, (입력 시) 국적, (입력 시) 재배 작목, 같은 언어 여부, 근사 거리
  - 정렬: 매칭 점수 — 같은 언어(+3), 같은 작목(+2), 같은/인접 읍·면(+2), 같은 국적(+1)
- 조건: **로그인 + 본인도 매칭 공개에 동의한 사용자만** 이웃 목록을 볼 수 있다(상호성 원칙)
- 이웃 항목 탭 → 그 사용자의 공개 게시글 목록

### 2.2 프로필 확장

| 필드 | 설명 |
| --- | --- |
| crop_type | 재배 작목(선택, 자유 텍스트 또는 목록. 특정 작목 코드를 로직에 하드코딩하지 않음) |
| is_matching_visible | 매칭 공개 동의(기본 false). true일 때만 이웃 목록에 노출되고 목록 열람 가능 |

### 2.3 게시글 표시 확장

- 게시글 카드·상세에 표시(모두 **근사·opt-in**):
  - **거리**: 뷰어의 선택 지역(읍·면) 중심좌표 ↔ 게시글 지역 중심좌표의 근사 거리("약 3km", 같은 읍·면이면 "우리 동네"). 일회성 위치 동의 시 뷰어 좌표만 세션 내 사용(저장 안 함)
  - **국적**: 작성자가 country_code를 입력했고 매칭 공개에 동의한 경우에만
- regions에 읍·면 **중심좌표**(centroid_lat/lng) 추가 — 공개 행정 지리정보이며 사용자 위치가 아니다

### 2.4 데이터·RLS 영향(구현 시 상세 설계)

- `regions`: centroid_lat/lng 컬럼 추가 + 홍성 읍·면 중심좌표 시딩(공개 지리정보)
- `profiles`: crop_type, is_matching_visible 추가
- 공개 뷰: `public_profiles`에 **opt-in 사용자에 한해** country_code·crop_type·region_id 노출 확장
  (D-006 개정 필요 — 현재는 id·nickname만. opt-in 조건부 노출로 원칙 유지)
- 이웃 목록 조회: is_matching_visible=true 행만 노출하는 뷰/정책. 열람자도 동의자여야 함(상호성)은
  RLS 또는 앱 로직+뷰 결합으로 강제

## 3. v1.3 대비 조정된 범위

**P0 추가**: 이웃 목록(2.1), 프로필 확장(2.2), 게시글 거리·국적 표시(2.3)
**P0 유지**: v1.3 P0 전부(게시글 작성·**수정**·삭제 포함)
**연기(제거)**: v1.3 P1 전부 — 지도 핀, 사진 업로드, 저장, 신고, PWA
**여전히 제외**: 실시간 위치 공유, DM·채팅, 자동번역, 소셜 로그인, 정확 좌표 수집

## 4. 여전히 지키는 원칙 (불변)

- 정확한 사용자 위치를 수집·저장·공개하지 않는다 (v1.3 §9)
- 국가·언어·작목을 코드에 하드코딩하지 않는다 (v1.3 §6.4)
- 국적은 선택 입력이며 접근 제한 수단이 아니다
- 모든 노출은 RLS로 강제한다 (UI 은닉으로 대체하지 않음)
- 읽기(게시판·생활정보)는 비로그인 허용. 이웃 목록만 로그인+동의 필요

## 5. 일정이 밀리면 (축소 순서 갱신)

1. 게시글 거리 표시 (2.3의 거리)
2. 매칭 점수 정렬(단순 같은-언어 필터로 격하)
3. 게시글 국적 표시
4. 이웃 목록 자체 (최후)

게시판 읽기·쓰기와 생활정보 탐색은 여전히 제거하지 않는다.

## 6. 변경 이력

| 버전 | 날짜 | 변경 내용 |
| --- | --- | --- |
| v1.4 | 2026-07-26 | 이웃 목록형 매칭 재도입(읍·면 단위, opt-in, 지도·정확위치·DM 없음), 프로필 작목·공개동의 추가, 게시글 근사 거리·국적 표시, P1 전면 연기 |
